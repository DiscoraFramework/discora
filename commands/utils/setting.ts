/** @format */

import { ButtonInteraction, SlashCommandBuilder } from "discord.js";
import { createSlashCommand } from "../../lib";

export const handleButtonClick = (interaction: ButtonInteraction) => {};
